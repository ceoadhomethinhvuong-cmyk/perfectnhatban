/.phpunit.cache
/node_modules
/public/hot
/public/storage
/storage/*.key
# NOTE: vendor/ and public/build/ are intentionally COMMITTED so the app
# can be git-deployed to shared hosting (Namecheap) without composer/npm.
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
/.claude
/perfect
/deploy
*.zip
